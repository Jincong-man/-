# 依赖目录（由 npm install 生成，不需要提交到版本控制）
node_modules/

# 测试覆盖率报告（自动生成，不需要提交）
tests/coverage/

# Claude Code 本地配置（包含个人路径等敏感信息，不要提交）
.claude/settings.local.json

# IDE 配置（部分含个人路径）
.vscode/

# 系统文件
.DS_Store
Thumbs.db
desktop.ini

# 环境变量文件（如果以后有的话）
.env
.env.local
.env.*.local
